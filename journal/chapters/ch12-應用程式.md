user程式可以放在虛擬位置用page映射
所以我們對user要寫一個新的user.ld 跟kernel.ld差不多，然後原本user程式要放在disk等kernel載入但我們這次是將user的執行檔嵌入kernel的data section中 所以比較大的工程在於改寫run.sh 另外我們也簡單寫了shell.c裡的main函式 目前只是無窮等待迴圈，最後也寫了user.c 表示使用者程式的執行流程

新的輸出結果如下 可以看到shell_bin的size和起始位置成功被列出
![[截圖 2026-07-10 晚上10.25.48.png]]
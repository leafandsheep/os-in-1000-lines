這是OS提到的重點章節，我們一開始先實作PCB結構，用struct的方式記錄課堂上提到的要素pid pc stack（存暫存器資訊）等等後面用來做context switch

context switch的部分，和課堂學到的不同，多了很多實作上的細節，我想最重要的會是PCB的實作，pcb裡面的sp會是contextswitch的重要對象，我的心得是 整個過程強調暫存器的狀態儲存與恢復，並實作了兩個不相干的程式彼此交互執行，實現多工的功能
成功執行
![[截圖 2026-07-09 下午4.57.19.png]]

但這還不夠 目前切換方式是A主動切B我們還要實作排程器yield函式讓切換到誰的功能獨立出來，所以需要多出process指標指向當前正在執行的process以及idleprocess來處理初始化和無process空間可用的兩個特殊情形，透過輪尋找出可跑的process然後檢查資格，最後用switch_context完成換出

也是達到相同成果
![[截圖 2026-07-10 凌晨12.17.59.png]]

除此之外 我們還要對kernel_entry做更新，考慮到時候sp要對應不同模式做更換，要有一個system call進來就交換到對應模式stack頂的機制 對yield和kernel_entry做更新因為sscratch 要在yield交還後要有對的stack首次使用 而後續若接著有多次trap 我們在kernel_entry的更新可以做到sscratch一直是對的stack頂
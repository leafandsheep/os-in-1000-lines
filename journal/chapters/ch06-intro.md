0707在這個章節要要實作一些標準函式庫的功能，包含字串操作和記憶體處理相關的函式（memcpy strcpy等等）
首先，在comon.h新增基本型別以及相關函式負責處了記憶體對齊（包含align_up和is_align）這邊是透過編譯器的內建函式，並且另外實作四個函式（memset memcpy strcpy strcmp）
本章節講義沒有需要測試所以只有完成程式實作 進入第七章 核心錯誤處理